const describe = require('mocha').describe
const it = require('mocha').it
const assert = require('chai').assert

describe('Test Refit Code Generator', function () {
  it('Dummy Test', function (done) {
    assert.isTrue(true)
    done()
  })
})
